/*import AsyncStorage from '@react-native-async-storage/async-storage';
import log from './coolog';

export default async function retrieveItem(key, value) {
  try {
    await AsyncStorage.removeItem(key, value);
  } catch (error) {
    log.fatal('Fatal error on storage');
  } finally {
    return true
  }
}*/